1. Install Node.js https://nodejs.org/en/download/
2. Execute (to install packages): 
    npm install
3a. Execute (to build project during development with hot reload):
    npm run build
3b. Execute (to build project for production):
    npm run dist